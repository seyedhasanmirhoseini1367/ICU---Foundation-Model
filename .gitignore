# data files — never push to GitHub
sample/
dataloader/all_stays/
dataloader/train_index.csv
dataloader/val_index.csv
dataloader/_tmp_split.csv
*.csv
*.ipynb

# Raw MIMIC source data (downloaded separately)
iii/
iv/

# Generated tokenizer artifacts (rebuild with tokenizer/build_vocab.py)
tokenizer/vocab.json
tokenizer/norm_stats.json
tokenizer/bin_edges.json

# Large HTML reference files (schema / handbook) — not part of the model code
*.html
!docs/*.html

# Pretrain internal split files
checkpoints/pretrain/_pretrain_train.csv
checkpoints/pretrain/_pretrain_val.csv

# Model checkpoints
checkpoints/

# Python cache
__pycache__/
*.pyc
*.pyo
